require 'spec_helper'

describe PrettyPhotoPortfolio do
  it 'has a version number' do
    expect(PrettyPhotoPortfolio::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
